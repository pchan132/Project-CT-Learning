# 🚀 LMS Quick Reference Guide

## คู่มือใช้งานด่วนสำหรับระบบ LMS

---

## 📋 สารบัญ
- [บัญชีทดสอบ](#บัญชีทดสอบ)
- [คำแนะนำสำหรับ Admin](#admin)
- [คำแนะนำสำหรับ Teacher](#teacher)
- [คำแนะนำสำหรับ Student](#student)
- [การแก้ไขไฟล์](#การแก้ไขไฟล์)
- [Artisan Commands ที่ใช้บ่อย](#artisan-commands)

---

## 🔐 บัญชีทดสอบ

| Role | Email | Password |
|------|-------|----------|
| **Admin** | admin@ct.ac.th | password |
| **Teacher 1** | teacher1@ct.ac.th | password |
| **Teacher 2** | teacher2@ct.ac.th | password |
| **Student 1** | student1@ct.ac.th | password |
| **Student 2** | student2@ct.ac.th | password |
| **Student 3-5** | student3-5@ct.ac.th | password |

---

## 👨‍💼 Admin

### เข้าสู่ระบบ
1. ไปที่ `http://localhost:8000/login`
2. ใช้ email: `admin@ct.ac.th` / password: `password`

### หน้าที่สามารถเข้าถึง:
- `/admin/dashboard` - Dashboard
- `/admin/users` - จัดการผู้ใช้
- `/admin/statistics` - สถิติ

### การจัดการผู้ใช้

#### สร้างผู้ใช้ใหม่:
1. ไปที่ `/admin/users`
2. คลิก "Create User"
3. กรอกข้อมูล:
   - Name: ชื่อผู้ใช้
   - Email: อีเมล
   - Password: รหัสผ่าน (ขั้นต่ำ 8 ตัว)
   - Role: เลือก student/teacher/admin
4. คลิก "Save"

#### แก้ไขผู้ใช้:
1. ไปที่ `/admin/users`
2. คลิก "Edit" ที่ผู้ใช้ที่ต้องการ
3. แก้ไขข้อมูล
4. คลิก "Update"

#### ลบผู้ใช้:
1. ไปที่ `/admin/users`
2. คลิก "Delete" (ไม่สามารถลบตัวเองได้)

---

## 👨‍🏫 Teacher

### เข้าสู่ระบบ
1. ไปที่ `http://localhost:8000/login`
2. ใช้ email: `teacher1@ct.ac.th` / password: `password`

### หน้าที่สามารถเข้าถึง:
- `/teacher/dashboard` - Dashboard (รายการคอร์สของครู)
- `/teacher/courses` - จัดการคอร์ส

---

### 📚 จัดการคอร์ส

#### สร้างคอร์สใหม่:
1. ไปที่ Dashboard
2. คลิก "Create New Course"
3. กรอก:
   - Title: ชื่อคอร์ส
   - Description: รายละเอียด
   - Cover Image: รูปปก (optional)
4. คลิก "Create"

#### แก้ไขคอร์ส:
1. ในรายการคอร์ส คลิก "Edit"
2. แก้ไขข้อมูล
3. คลิก "Update"

#### ลบคอร์ส:
1. คลิก "Delete" (จะลบทุกอย่างใน Course)

---

### 📖 จัดการ Module

#### เพิ่ม Module:
1. เข้าคอร์ส
2. คลิก "Add Module"
3. กรอก:
   - Title: ชื่อบท
   - Description: รายละเอียด
   - Order: ลำดับ (1, 2, 3...)
4. คลิก "Save"

#### แก้ไข/ลบ Module:
1. คลิก "Edit" หรือ "Delete" ที่ Module

---

### 📄 จัดการ Lesson

#### เพิ่ม Lesson:
1. เข้า Module
2. คลิก "Add Lesson"
3. กรอก:
   - Title: ชื่อบทเรียน
   - Content Type: เลือกประเภท
     - **TEXT**: เนื้อหาแบบข้อความ (ใช้ Rich Text Editor)
     - **VIDEO**: ลิงก์ YouTube (embed URL)
     - **PDF**: อัพโหลดไฟล์ PDF
     - **PPT**: อัพโหลดไฟล์ PowerPoint
   - Order: ลำดับ
4. คลิก "Save"

**ตัวอย่าง YouTube Embed URL:**
```
https://www.youtube.com/embed/VIDEO_ID
```

#### แก้ไข/ลบ Lesson:
1. คลิก "Edit" หรือ "Delete"

---

### 📝 จัดการ Quiz

#### สร้าง Quiz:
1. เข้า Module
2. คลิก "Create Quiz"
3. กรอก:
   - Title: ชื่อแบบทดสอบ
   - Description: คำอธิบาย
   - Passing Score: คะแนนผ่าน (0-100)
   - Time Limit: เวลาจำกัด (นาที, optional)
4. คลิก "Create"

#### เพิ่มคำถาม:
1. หลังสร้าง Quiz จะไปที่หน้า "Add Questions"
2. กรอกคำถาม:
   - Question Text: คำถาม
   - Answers: ตัวเลือก (ขั้นต่ำ 2 ข้อ)
   - Correct Answer: เลือกคำตอบที่ถูก
3. คลิก "Add Question"
4. สามารถเพิ่มคำถามได้หลายข้อ

**ตัวอย่าง:**
```
Question: Laravel เป็นอะไร?
Answers:
  □ Framework ของ PHP     ← เลือกนี่เป็นคำตอบที่ถูก
  □ ภาษาโปรแกรมมิ่ง
  □ Database
  □ Operating System
```

#### แก้ไขคำถาม:
1. คลิก "Edit" ที่คำถาม
2. แก้ไขและ Save

#### ลบคำถาม:
1. คลิก "Delete"

#### ดูผลคะแนนของนักเรียน:
1. เข้า Quiz
2. ดูในส่วน "Student Results"

---

## 👨‍🎓 Student

### เข้าสู่ระบบ
1. ไปที่ `http://localhost:8000/login`
2. ใช้ email: `student1@ct.ac.th` / password: `password`

### หน้าที่สามารถเข้าถึง:
- `/student/dashboard` - Dashboard
- `/student/courses` - คอร์สทั้งหมด
- `/student/courses/my-courses` - คอร์สที่ลงทะเบียน
- `/student/certificates` - ใบประกาศของฉัน

---

### 📚 ลงทะเบียนเรียน

#### ดูคอร์สทั้งหมด:
1. ไปที่ "Browse Courses"
2. ดูรายการคอร์สที่มี

#### ลงทะเบียนคอร์ส:
1. คลิกเข้าคอร์สที่ต้องการ
2. คลิก "Enroll in this Course"
3. คอร์สจะปรากฏใน "My Courses"

#### ยกเลิกการลงทะเบียน:
1. เข้าคอร์ส
2. คลิก "Unenroll"

---

### 📖 เรียนบทเรียน

#### เริ่มเรียน:
1. ไปที่ "My Courses"
2. เลือกคอร์ส
3. เลือก Module
4. คลิกที่ Lesson

#### Mark Complete:
1. อ่าน/ดูเนื้อหาจนจบ
2. คลิก "Mark as Complete"
3. Progress จะอัพเดทอัตโนมัติ

**Progress คำนวณจาก:**
```
Progress = (Lessons ที่เรียนเสร็จ / Lessons ทั้งหมด) × 100
```

---

### 📝 ทำแบบทดสอบ

#### เริ่มทำ Quiz:
1. เข้า Module ที่มี Quiz
2. คลิกที่ Quiz
3. คลิก "Start Quiz"

#### ทำข้อสอบ:
1. อ่านคำถามและเลือกคำตอบ
2. คลิก "Submit Quiz" เมื่อทำเสร็จ

#### ดูผลคะแนน:
1. หลังส่งคำตอบจะเห็นผลทันที:
   - คะแนนที่ได้
   - ผ่านหรือไม่ผ่าน
   - เวลาที่ใช้
   - คำตอบที่ถูก/ผิด

#### ทำใหม่:
1. สามารถทำซ้ำได้ (ไม่จำกัดครั้ง)
2. เก็บคะแนนสูงสุด

---

### 🎓 ขอใบประกาศนียบัตร

#### เงื่อนไขการได้ Certificate:
- ✅ เรียนครบทุก Lesson (Progress 100%)
- ✅ ผ่านทุก Quiz ในคอร์ส

#### วิธีขอ Certificate:
1. เข้าคอร์สที่เรียนเสร็จแล้ว
2. คลิก "Get Certificate" (จะปรากฏเมื่อครบเงื่อนไข)
3. ระบบจะสร้าง PDF ให้อัตโนมัติ

#### ดาวน์โหลด Certificate:
1. ไปที่ "My Certificates"
2. คลิก "Download PDF"

---

## 🛠️ การแก้ไขไฟล์

### ไฟล์สำคัญที่ควรรู้:

#### Models (ข้อมูล):
```
app/Models/
├── User.php           # ผู้ใช้
├── Course.php         # คอร์ส
├── Module.php         # บท
├── Lesson.php         # บทเรียน
├── Quiz.php           # แบบทดสอบ
├── Question.php       # คำถาม
├── Answer.php         # คำตอบ
├── QuizAttempt.php    # ผลการทำแบบทดสอบ
└── Certificate.php    # ใบประกาศ
```

#### Controllers (Logic):
```
app/Http/Controllers/
├── Admin/
│   └── AdminController.php         # Admin functions
├── Teacher/
│   ├── CourseController.php        # จัดการคอร์ส
│   ├── ModuleController.php        # จัดการ Module
│   ├── LessonController.php        # จัดการ Lesson
│   ├── QuizController.php          # จัดการ Quiz
│   └── QuestionController.php      # จัดการคำถาม
└── Student/
    ├── CourseController.php        # ดูคอร์ส, enroll, เรียน
    ├── QuizController.php          # ทำแบบทดสอบ
    └── CertificateController.php   # ขอใบประกาศ
```

#### Views (หน้าเว็บ):
```
resources/views/
├── admin/           # หน้า Admin
├── teacher/         # หน้า Teacher
├── student/         # หน้า Student
└── certificates/    # Template PDF
```

#### Routes (เส้นทาง URL):
```
routes/web.php       # กำหนด URL ทั้งหมด
```

---

## ⚡ Artisan Commands

### การใช้งานพื้นฐาน:

#### รัน Server:
```bash
php artisan serve
# เปิดที่ http://localhost:8000
```

#### Database:
```bash
# รัน Migration ทั้งหมด
php artisan migrate

# รัน Migration + ลบข้อมูลเดิม
php artisan migrate:fresh

# รัน Migration + Seed ข้อมูลทดสอบ
php artisan migrate:fresh --seed
```

#### สร้าง Symbolic Link (สำหรับ Storage):
```bash
php artisan storage:link
```

#### Cache:
```bash
# Clear Cache
php artisan cache:clear

# Clear Config
php artisan config:clear

# Clear Route
php artisan route:clear

# Clear View
php artisan view:clear

# Clear ทั้งหมด
php artisan optimize:clear
```

### สร้างไฟล์ใหม่:

```bash
# สร้าง Model + Migration
php artisan make:model ModelName -m

# สร้าง Controller
php artisan make:controller ControllerName

# สร้าง Controller แบบ Resource
php artisan make:controller ControllerName --resource

# สร้าง Migration
php artisan make:migration create_table_name

# สร้าง Seeder
php artisan make:seeder SeederName

# สร้าง Middleware
php artisan make:middleware MiddlewareName
```

---

## 🔍 ตรวจสอบระบบ

### ดู Routes ทั้งหมด:
```bash
php artisan route:list
```

### ดู Database Schema:
```bash
php artisan schema:dump
```

### ทดสอบ Email:
```bash
php artisan tinker
>>> Mail::raw('Test', function($msg) { 
    $msg->to('test@example.com')->subject('Test'); 
});
```

---

## 🐛 แก้ไขปัญหา

### ปัญหา: ไม่แสดงรูปที่อัพโหลด
```bash
php artisan storage:link
chmod -R 775 storage
```

### ปัญหา: Migration Error
```bash
php artisan migrate:fresh --seed
```

### ปัญหา: 500 Error
```bash
# เปิดดู Error
แก้ไขใน .env: APP_DEBUG=true

# ดู Log
tail -f storage/logs/laravel.log
```

### ปัญหา: Permission Denied
```bash
# Windows
icacls storage /grant Users:F /t
icacls bootstrap\cache /grant Users:F /t

# Linux/Mac
chmod -R 775 storage bootstrap/cache
```

---

## 📞 ติดต่อ

- 📧 Email: support@ct.ac.th
- 📚 เอกสารเพิ่มเติม: `/context/docs/`

---

## 🎯 เอกสารเพิ่มเติม

- [LMS-COMPLETE-GUIDE.md](./LMS-COMPLETE-GUIDE.md) - เอกสารครบถ้วน
- [QUIZ-SYSTEM-GUIDE.md](./QUIZ-SYSTEM-GUIDE.md) - ระบบแบบทดสอบ
- [CERTIFICATE-SYSTEM-GUIDE.md](./CERTIFICATE-SYSTEM-GUIDE.md) - ระบบใบประกาศ
- [ARCHITECTURE.md](./ARCHITECTURE.md) - สถาปัตยกรรมระบบ

---

**Last Updated:** 24 พฤศจิกายน 2025
